
var nodo= document.querySelector (".job-search-result");
for (var i=0; i<nodo.length; i++) {
    querySelector("h3").innerText       // Ejecutar acciones sobre el nodo listaNodosOperadores[i].
       }


document.querySelectorAll(".job-search-result").map(({ children }) => ({
        console.log("entra")
       }));